name: Publish Website
on:
  push:
jobs:
  web-deploy:
    name: 🚀 Deploy Website Every Commit
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get Latest Code
        uses: actions/checkout@v3

      - name: 📂 Sync Files
        uses: SamKirkland/web-deploy@v1
        with:
          target-server: ssh.cluster027.hosting.ovh.net
          remote-user: clanbap
          private-ssh-key: ${{ secrets.OVH_HOSTING_PASSWORD }}
          ssh-port: 22
          destination-path: ~/
          rsync-options: --dry-run --archive --verbose --compress --delete-after --human-readable --exclude=.git* --exclude=.git/ --exclude=README.md --exclude=readme.md --exclude=.gitignore
