import React from "react";
import ReactCountryFlag from "react-country-flag";

const Player = ({player}) => {
    console.log(player)
    return (
        <>
            
        </>
    );
};

export default Player;
